# Train-Price-Prediction-MLOPS
This project focuses on predicting train fares using machine learning, similar to common flight price prediction tasks. The dataset contains various features related to train journeys (such as source, destination, date, and class), and the goal is to build regression models that can accurately estimate ticket prices.

there is change in this 
check for this